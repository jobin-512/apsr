<script lang="ts">
  let currency = $state<'USD' | 'GBP' | 'CAD'>('USD');

  const symbols = {
    USD: "$",
    GBP: "£",
    CAD: "$"
  };

  const plans = [
    {
      name: "Starter",
      desc: "Perfect for small businesses getting started",
      price: { USD: 1200, GBP: 950, CAD: 1600 },
      highlight: false
    },
    {
      name: "Intermediate",
      desc: "Best for growing businesses",
      price: { USD: 2500, GBP: 1995, CAD: 3350 },
      highlight: true
    },
    {
      name: "Enterprise",
      desc: "For high-scale, custom builds",
      price: { USD: 5000, GBP: 3995, CAD: 6700 },
      highlight: false
    }
  ];
</script>

<section id="pricing" class="bg-[#02140a] text-white py-32">

  <div class="max-w-7xl mx-auto px-6 md:px-20">

    <!-- HEADER -->
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-16 gap-6">

      <div>
        <h2 class="text-[42px] md:text-[64px] font-semibold">
          Choose your website package
        </h2>
        <p class="text-gray-400 mt-3 max-w-xl">
          One-time investment. Built to perform, scale, and convert.
        </p>
      </div>

      <!-- CURRENCY TOGGLE -->
      <div class="flex gap-2 bg-black/30 border border-white/10 rounded-full p-1">
        {#each ['USD','GBP','CAD'] as cur}
          <!-- svelte-ignore event_directive_deprecated -->
          <button
            on:click={() => currency = cur}
            class="px-4 py-2 text-xs rounded-full transition-all"
            class:bg-lime-400={currency === cur}
            class:text-black={currency === cur}
            class:text-gray-400={currency !== cur}
          >
            {cur}
          </button>
        {/each}
      </div>

    </div>

    <!-- CARDS -->
    <div class="grid md:grid-cols-3 gap-8">

      {#each plans as plan}

        <div
          class="relative group rounded-2xl border border-white/10 p-8 bg-black/30 backdrop-blur-xl transition-all duration-300 hover:scale-[1.03]"
          class:ring-2={plan.highlight}
          class:ring-lime-400={plan.highlight}
        >

          <!-- BEST TAG -->
          {#if plan.highlight}
            <div class="absolute -top-2 left-1/2 -translate-x-1/2 bg-lime-400 text-black text-xs px-3 py-1 rounded-full font-bold tracking-widest">
              BEST VALUE
            </div>
          {/if}

          <!-- NAME -->
          <h3 class="text-xl font-semibold">
            {plan.name}
          </h3>

          <p class="text-gray-400 text-sm mt-2">
            {plan.desc}
          </p>

          <!-- PRICE -->
          <div class="mt-6">

            <p class="text-4xl font-bold text-lime-400">
              {symbols[currency]}{plan.price[currency]}
            </p>

            <p class="text-xs text-gray-500 mt-1">
              One-time payment
            </p>

          </div>

          <!-- FEATURES PREVIEW -->
          <ul class="mt-6 space-y-3 text-sm text-gray-300">
            <li>✔ Custom Design</li>
            <li>✔ Mobile Optimized</li>
            <li>✔ SEO Ready Structure</li>
            <li>✔ Fast Performance</li>
          </ul>

          <!-- CTA -->
          <a
            href="/get-started"
            class="mt-8 block text-center bg-lime-400 text-black font-bold text-xs tracking-widest uppercase px-6 py-4 rounded-full hover:scale-105 transition"
          >
            Get Started
          </a>

        </div>

      {/each}

    </div>

  </div>
</section>