<script lang="ts">
  const steps = [
    {
      title: "Discovery & Strategy",
      desc: "We understand your business, audience, and goals to craft a winning website strategy."
    },
    {
      title: "UI/UX Design",
      desc: "We design high-converting interfaces that are visually stunning and easy to use."
    },
    {
      title: "Development",
      desc: "We build fast, scalable, and fully responsive websites with clean code."
    },
    {
      title: "Testing & Optimization",
      desc: "We test across devices and optimize for speed, SEO, and performance."
    },
    {
      title: "Launch",
      desc: "We deploy your website seamlessly with zero downtime."
    },
    {
      title: "Scale & Support",
      desc: "We help you grow with ongoing improvements and optimizations."
    }
  ];

  // ✅ CLEAN SVELTE ACTION (NO BUGS)
  function inView(node: HTMLElement) {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          node.classList.add('show');
          observer.unobserve(node);
        }
      },
      {
        threshold: 0,
        rootMargin: "0px 0px -100px 0px"
      }
    );

    observer.observe(node);

    // 🔥 fallback (guarantees visibility)
    const fallback = setTimeout(() => {
      node.classList.add('show');
    }, 300);

    return {
      destroy() {
        observer.disconnect();
        clearTimeout(fallback);
      }
    };
  }
</script>

<section class="relative bg-[#020f05] text-white py-32 overflow-hidden">

  <!-- 🌟 ambient glow -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-[10%] left-[10%] w-[500px] h-[500px] bg-lime-400/10 blur-[140px] rounded-full"></div>
    <div class="absolute bottom-[0%] right-[10%] w-[400px] h-[400px] bg-green-900/30 blur-[120px] rounded-full"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-6 md:px-20">

    <!-- HEADER -->
    <div class="mb-20 max-w-2xl">
      <h2 class="text-[42px] md:text-[64px] leading-tight font-semibold">
        Our process, built for results
      </h2>
      <p class="text-gray-400 mt-4">
        A proven system that turns ideas into high-performing digital experiences.
      </p>
    </div>

    <!-- 🔥 MOBILE (TIMELINE) -->
    <div class="md:hidden relative">

      <!-- vertical line -->
      <div class="absolute left-3 top-0 bottom-0 w-[2px] bg-white/10"></div>

      <div class="space-y-12">
        {#each steps as step, i}

          <div
            use:inView
            class="relative pl-10 opacity-100 translate-y-10"
            style="transition-delay:{i * 100}ms"
          >

            <!-- dot -->
            <div class="absolute left-0 top-1 w-6 h-6 rounded-full bg-lime-400/20 flex items-center justify-center text-xs text-lime-400">
              {i + 1}
            </div>

            <!-- card -->
            <div class="p-5 rounded-xl border border-white/10 bg-black/30 backdrop-blur-xl transition-all duration-500">
              <h3 class="text-lg font-semibold mb-2">{step.title}</h3>
              <p class="text-gray-400 text-sm leading-relaxed">{step.desc}</p>
            </div>

          </div>

        {/each}
      </div>

    </div>

    <!-- 🔥 DESKTOP (FLOW CARDS) -->
    <div class="hidden md:grid grid-cols-2 gap-10">

      {#each steps as step, i}

        <div
          use:inView
          class="group relative opacity-100 translate-y-10"
          style="transition-delay:{i * 120}ms"
        >

          <!-- connecting line -->
          {#if i % 2 === 0}
            <div class="absolute right-[-20px] top-1/2 w-10 h-[2px] bg-lime-400/30"></div>
          {/if}

          <!-- card -->
          <div class="relative p-8 rounded-2xl border border-white/10 bg-black/30 backdrop-blur-xl transition-all duration-300 group-hover:scale-[1.04] group-hover:border-lime-400/40">

            <!-- step number -->
            <div class="text-lime-400 text-sm mb-4">
              {String(i + 1).padStart(2, "0")}
            </div>

            <h3 class="text-xl font-semibold mb-3">
              {step.title}
            </h3>

            <p class="text-gray-400 text-sm leading-relaxed">
              {step.desc}
            </p>

            <!-- glow -->
            <div class="absolute inset-0 rounded-2xl opacity-0 group-hover:opacity-100 transition duration-500 bg-lime-400/5 blur-xl"></div>

          </div>

        </div>

      {/each}

    </div>

  </div>

</section>