<script lang="ts">
  let hovered = $state(false);
</script>

<section class="relative bg-[#020f05] text-white py-32 overflow-hidden">

  <!-- 🌟 ambient glow -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-[20%] left-[20%] w-[500px] h-[500px] bg-lime-400/10 blur-[140px] rounded-full"></div>
    <div class="absolute bottom-[10%] right-[10%] w-[400px] h-[400px] bg-green-900/30 blur-[120px] rounded-full"></div>
  </div>

  <!-- 🔥 subtle grid overlay -->
  <div class="absolute inset-0 opacity-[0.04] bg-[linear-gradient(to_right,#fff_1px,transparent_1px),linear-gradient(to_bottom,#fff_1px,transparent_1px)] bg-[size:60px_60px]"></div>

  <div class="relative max-w-5xl mx-auto px-6 md:px-20 text-center">

    <!-- HEADLINE -->
    <h2 class="text-[42px] md:text-[72px] leading-tight font-semibold">
      Ready to build something <span class="text-lime-400">exceptional?</span>
    </h2>

    <p class="text-gray-400 mt-6 text-lg max-w-2xl mx-auto">
      Not just a website — a high-converting digital asset designed to grow your business.
    </p>

    <!-- 🔥 VALUE STRIP -->
    <div class="flex flex-wrap justify-center gap-6 mt-10 text-sm text-gray-300">

      <div class="px-4 py-2 rounded-full border border-white/10 bg-white/5 backdrop-blur">
        ⚡ Fast turnaround
      </div>

      <div class="px-4 py-2 rounded-full border border-white/10 bg-white/5 backdrop-blur">
        🎯 Conversion-focused
      </div>

      <div class="px-4 py-2 rounded-full border border-white/10 bg-white/5 backdrop-blur">
        🚀 Scalable architecture
      </div>

    </div>

    <!-- 🔥 CTA BUTTONS -->
    <div class="mt-14 flex flex-col sm:flex-row justify-center gap-6">

      <!-- PRIMARY CTA -->
      <a
        href="/get-started"
        on:mouseenter={() => hovered = true}
        on:mouseleave={() => hovered = false}
        class="relative inline-flex items-center justify-center gap-2 px-10 py-4 rounded-full font-bold tracking-widest uppercase text-sm bg-lime-400 text-black overflow-hidden transition-all duration-300 hover:scale-105"
      >

        <!-- glow pulse -->
        <span class="absolute inset-0 bg-white/20 opacity-0 group-hover:opacity-100 transition"></span>

        Get Started

        <svg class="w-4 h-4 transition-transform duration-300"
          style="transform: translateX({hovered ? '6px' : '0px'})"
          viewBox="0 0 12 12" fill="none">
          <path d="M1 6H11M11 6L6 1M11 6L6 11"
            stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>

      </a>

      <!-- SECONDARY CTA -->
      <a
        href="/contact"
        class="px-10 py-4 rounded-full border border-white/15 text-sm tracking-widest uppercase hover:border-lime-400 hover:text-lime-400 transition"
      >
        Talk to an Expert
      </a>

    </div>

    <!-- 🔥 TRUST LINE -->
    <p class="mt-10 text-xs text-gray-500 tracking-widest uppercase">
      No commitment • Free consultation • Transparent pricing
    </p>

  </div>

</section>

<style>
  a:hover {
    box-shadow: 0 0 40px rgba(163, 230, 53, 0.25);
  }
</style>